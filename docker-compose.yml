version: '3.8'

services:
  zggsds:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - RUST_LOG=info
      - JWT_SECRET
      - ADMIN_PASSWORD
    restart: unless-stopped
